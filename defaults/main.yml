---
# DNS Related vars
RESOLV_NAMESERVERS:
  - 1.1.1.1
  - 1.0.0.1
RESOLV_SEARCH: []
RESOLV_SORTLIST: []
RESOLV_OPTIONS: []
RESOLV_DOMAIN: ''

# Set /etc/resolv.conf read-only with the `i` file attibute
RESOLV_LOCK: false

# Define if need vm guest tools.
# Can be `VMware` ; `kvm` ; or `none`
# (Work with Redhat And Debian)
COMMOM_VM_HOST: "{{ ansible_virtualization_type | default('none') }}"
